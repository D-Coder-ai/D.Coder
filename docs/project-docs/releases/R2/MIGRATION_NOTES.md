# Migration Notes — R1 → R2

- Add Vault/KMS; migrate provider secrets to secretRef
- Introduce archival: set retention defaults; backfill if needed
- Update service contracts and event schemas; keep v1 compatibility
- Keep guardrails detect-only; prepare policy interfaces for R3 block mode
