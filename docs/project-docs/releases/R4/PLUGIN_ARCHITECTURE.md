# Plugin Architecture — R4 (Marketplace)

- Marketplace UX: discovery, permissions, install/enable/disable, update, rollback
- Versioning: semver constraints, compatibility checks
- Audit: plugin actions captured with actor and timestamps
- Tenant snapshots: config versions for rollback
