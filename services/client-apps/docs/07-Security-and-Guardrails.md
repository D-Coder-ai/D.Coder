 # Security and Guardrails
 
 - OIDC login with Logto; secure cookie/session storage
 - Attach required headers to API calls; never log secrets
 - Guardrails: UI warnings only; no client-side blocking in R1
 - Rate limiting enforced at Kong
