 # Proxy Endpoints (selected)
 
 - `GET /health` (per service)
 - `POST /v1/llm/*` → upstream services per route mapping
 - Admin: `GET http://localhost:8001/services|routes|plugins|status`
 
 Conventions and headers follow global Service Contracts.
