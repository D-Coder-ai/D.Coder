 # Integrations
 
 - Proxies requests to all platform services
 - Coordinates with LiteLLM by keeping LLM routes separate
 - Consumes platform allowlists and policies from infra configs
 
 Call rules: attach and normalize required headers; enforce rate limits per tenant.
