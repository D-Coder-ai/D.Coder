 # APIs
 
 - GET `/health`
 - GET `/metrics`
 - POST `/v1/chat/completions` (OpenAI-compatible)
 - POST `/key/generate` (virtual keys)
 
 Headers and error envelope follow global conventions where applicable; bearer auth for API.
