version: '3.8'

services:
  llmops:
    build: .
    ports:
      - "8081:8081"
    environment:
      SERVICE_NAME: llmops
      SERVICE_PORT: 8081
      DEBUG: "true"
    volumes:
      - ./src:/app/src
